<div>
    <div id="offline_tip">
        <div class="window">
            <h1 style="font-weight: 200;font-size: 3rem">{{ tr('Waiting for network') }}...</h1>
            <p>{{ config('app.name', 'The application') . ' ' . tr('can not run if you are offline.') }}</p>
        </div>

    </div>

</div>
